import React from "react";
import { BarChart, Bar, XAxis, YAxis, Tooltip, CartesianGrid } from "recharts";

const PovertyChart = ({ data }) => {
  return <div id="barchart-section">Bar chart here</div>;
};

export default PovertyChart;
